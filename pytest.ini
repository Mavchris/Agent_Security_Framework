[pytest]
markers =
    integration: makes real network calls against live third-party APIs, or needs heavy optional local dependencies (e.g. downloaded translation models) not installed by default; excluded by default (run with -m integration)
addopts = -m "not integration"
